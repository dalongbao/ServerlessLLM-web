
"use client";

/* Placeholder.  Add props, state, or remove `"use client"` if you turn it
   into a server component later. */
export default function TopPanel(
  { className = "" }: { className?: string },
) {
  return <div className={className}></div>;
}
